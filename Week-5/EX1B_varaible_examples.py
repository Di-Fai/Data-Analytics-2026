customer_id = 1001  # I used a number because customer IDs are usually numeric, but it could also be text if it includes letters.

customer_name = "Dimitri Nji"  # I wrote first name + last name together, but it could be better as separate first_name and last_name variables.

customer_gender = "Male"  # I used text, but the system may need specific options like M, F, or Other.

customer_date_of_birth = "1998-05-15"  # I used text in YYYY-MM-DD format, but Python also has date formats for more advanced use.

drivers_license_number = "D123456789"  # I used text because license numbers can include letters and numbers.

auto_policy_number = "AUTO987654"  # I used text because policy numbers can include letters, numbers, or dashes.

my_name = "Dimitri Nji"  # I used my full name as one text value.

hometown = "New Castle, Delaware"  # I used city and state together, but they could be separated into city and state variables.


