customer_id = 1001  # I used a number because customer IDs are usually numeric, but it could also be text if it includes letters.

customer_name = "Dominique Orcha"  # I wrote first name + last name together, but it could be better as separate first_name and last_name variables.

customer_gender = "Male"  # I used text, but the system may need specific options like M, F, or Other.

customer_date_of_birth = "1998-03-18"  # I used text in YYYY-MM-DD format, but Python also has date formats for more advanced use.

drivers_license_number = "D123456789"  # I used text because license numbers can include letters and numbers.

auto_policy_number = "AUTO987654"  # I used text because policy numbers can include letters, numbers, or dashes.

my_name = "Dimitri Nji"  # I used my full name as one text value.

hometown = "Douala, Litoral"  # I used city and region together, but they could be separated into city and region variables.



import keyword
print(keyword.kwlist)

'''
a) What is the full list of reserved words that can’t be used for variable names?

    'False', 'None', 'True', 'and', 'as', 'assert', 'async', 'await', 'break', 'class', 'continue', 'def', 'del',
    'elif', 'else', 'except', 'finally', 'for', 'from', 'global', 'if', 'import', 'in', 'is', 'lambda', 'nonlocal', 'not',
    'or', 'pass', 'raise', 'return', 'try', 'while', 'with', 'yield'

b) Definitions of 5 reserved words:
    -def:    Used to define a function in Python. Functions allow you to group code into reusable blocks that perform a specific task.


    -if:     Used to create a conditional statement. It allows the program to make decisions and run code only when a condition is True.


    -for:    Used to create a loop that iterates over a sequence (like a list or string). It repeats a block of code for each item in the sequence.


    -return: Used inside a function to send a value back to where the function was called. It ends the function execution and gives back the result.


    -class:  Used to define a blueprint for creating objects. It is part of object-oriented programming and allows you to group data and functions together.


'''








